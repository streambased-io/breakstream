  
  grafana:
    image: grafana/grafana-oss:latest
    hostname: grafana
    container_name: grafana
    # volumes:
    #   - ./grafana/infinity-datasource:/var/lib/grafana/plugins/infinity-datasource
    # environment:
    #   - GF_PLUGINS_ALLOW_LOADING_UNSIGNED_PLUGINS=trino-datasource
    # volumes:
    #   - grafana-data:/var/lib/grafana
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning
    environment:
      GF_INSTALL_PLUGINS: yesoreyeram-infinity-datasource
      INFINITY_BASE_URL: http://hyperstream:9088
      INFINITY_BASIC_AUTH_USER: sbpk_12345678
      INFINITY_BASIC_AUTH_PASSWORD: sbsk_12345678
      INFINITY_ALLOWED_HOST: http://hyperstream:9088
    ports:
      - '7070:3000'